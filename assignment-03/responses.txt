1. DNS turns a domain name into an IP address. DNS eliminates a need to memorize IP addresses to access websites as names are much easier to remember.
2. A cookie is a browser object that stores information about a user.
3. The three essential parts of a URL are the protocol, domain, and path.
